/**
 * Pick Components Examples — entry point.
 *
 * The shell bootstrap now lives under `examples/src/bootstrap/` so the
 * playground can grow toward a feature-oriented structure without changing the
 * external build entry yet.
 */
import "./bootstrap/main.js";
